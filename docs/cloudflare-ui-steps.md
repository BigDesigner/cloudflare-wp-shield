# Cloudflare UI Steps (Free Plan)

1. Cloudflare Dashboard → select your domain.
2. Security → WAF → Custom rules.
3. Create rule (name + expression + action).
4. Arrange order as recommended in `rules/ruleset-free-plan.md`.
5. Deploy.
6. Validate via Security → Events (allow 1–5 minutes for events to appear).
